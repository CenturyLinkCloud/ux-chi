---
title: Spinner
scripts:
  - js/ce/ux-chi-ce
styles:
  - assets/styles/spinner-ce

---
p.-text Spinners are animated circles that let users know when information is saving or loading.

h3 Examples
p.-text To render a spinner, use the tag <code>&lt;chi-spinner&gt;</code>.

h3 Backdrop
p.-text To render spinners in backdrop use <code>backdrop</code> attribute to overlay content similar to a modal.
h4 Base
.example
  div.-p--6(style="height:25rem;")
    chi-spinner(backdrop, color='inverse')
:code(lang="html")
  <chi-spinner backdrop color='inverse'></chi-spinner>

h4 Inverse
p.-text Set the backdrop attribute to <code>inverse</code> to render spinners on light backgrounds.
.example
  div.-p--6(style="height:25rem;")
    chi-spinner(backdrop='inverse', color='primary')
:code(lang="html")
  <chi-spinner backdrop='inverse' color='primary'></chi-spinner>

h3 Spinner Sizes
p.-text
  | By default, spinners render at 16x16px (<code>sm</code>).
p.-text
  | Set size attribute to render spinners larger or smaller: <code>xs</code>,
  | <code>sm</code>, <code>sm--2</code>, <code>sm--3</code>, <code>md</code>, <code>lg</code>, <code>xl</code>, <code>xxl</code>.
.example.-mb--3
  .-p--3
    .a-grid.-align-items--end
      each size in ['xs', 'sm', 'sm--2', 'sm--3', 'md', 'lg', 'xl', 'xxl']
        div.-p--2
          chi-spinner(size=size)
          p.-text.-text--center= size
  :code(lang="html")
    <!-- xs : 12x12px -->
    <chi-spinner size='xs'></chi-spinner>
    <!-- sm : 16x16px -->
    <chi-spinner size='sm'></chi-spinner>
    <!-- sm--2 : 20x20px -->
    <chi-spinner size='sm--2'></chi-spinner>
    <!-- sm--3 : 24x24px -->
    <chi-spinner size='sm--3'></chi-spinner>
    <!-- md : 32x32px -->
    <chi-spinner size='md'></chi-spinner>
    <!-- lg : 64x64px -->
    <chi-spinner size='lg'></chi-spinner>
    <!-- xl : 96x96px -->
    <chi-spinner size='xl'></chi-spinner>
    <!-- xxl : 128x128px -->
    <chi-spinner size='xxl'></chi-spinner>

h3 Spinner Colors
p.-text
  | To change the color of a spinner use color attribute.
.example.-mb--3
  .-m--3
    .a-grid.-align-items--center
      each type in ['primary', 'success', 'warning', 'danger', 'muted']
        .-p--2
          chi-spinner(color=type, size='md')
          p.-text(class=`-text--${type}`)
            = `${type}`
  :code(lang="html")
    <chi-spinner color='primary'></chi-spinner>
    <chi-spinner color='success'></chi-spinner>
    <chi-spinner color='warning'></chi-spinner>
    <chi-spinner color='danger'></chi-spinner>
    <chi-spinner color='muted'></chi-spinner>
